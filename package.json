{
  "name": "mekong-tales",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "kho-tri-thuc": "node scripts/tao-kho-tri-thuc.mjs",
    "dev:chat": "wrangler pages dev dist --ai AI",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "astro": "^7.1.1",
    "marked": "^18.0.7"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "wrangler": "^4.114.0"
  }
}
