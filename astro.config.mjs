// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Đổi khi có tên miền riêng. Không để dấu / ở cuối.
  site: 'https://mekongtales.pages.dev',
  output: 'static',
});
